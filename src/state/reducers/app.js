import {} from "../constants/appConstants";

const INITIAL_STATE = {
    count:0
};

const app = (state = INITIAL_STATE, action) => {

    switch (action.type) {
      
        default: return state;
    }

};

export default app;